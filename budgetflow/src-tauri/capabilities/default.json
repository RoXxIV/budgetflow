{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Fenêtre principale : API cœur uniquement (pas d'accès système côté JS)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
