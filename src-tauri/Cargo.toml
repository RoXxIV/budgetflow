[package]
name = "budgetflow2"
version = "2.0.0"
description = "BudgetFlow — budget mensuel personnel"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
